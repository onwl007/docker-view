http:
  addr: ":8080"

log:
  level: "info"

docker:
  host: "unix:///var/run/docker.sock"

web:
  dir: "./web/dist"

security:
  requireAuthentication: false
  authToken: ""

audit:
  enabled: true
  maxEvents: 500
