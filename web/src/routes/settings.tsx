import { FeaturePlaceholder } from '@/components/app/feature-placeholder'

export function SettingsPage() {
  return (
    <FeaturePlaceholder
      title="Settings"
      description="Settings is reserved for Phase 4, where Docker connection, security, notifications, and appearance configuration will be introduced."
      phase="Phase 4"
    />
  )
}
